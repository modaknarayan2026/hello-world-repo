name: Docker Simple Workflow

on: push

jobs:
  docker-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t hello-docker .

      - name: Run Docker container
        run: docker run hello-docker
